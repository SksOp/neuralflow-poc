import { Int, Float } from "@/packages/typewriter";
import { ArgsInstance } from "../base";
export const args: ArgsInstance[] = [
  {
    name: "alpha",
    isRequired: false,
    defaultValue: Float.of(1.0),
  },
];
